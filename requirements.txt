# Core FastAPI stack
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# Optional database (keep minimal)
sqlalchemy==2.0.23
psycopg2-binary==2.9.9

# Utilities
python-multipart==0.0.6
prometheus-client==0.19.0

# Remove numpy and pandas - they're not needed and cause deployment issues
# Monitoring only
